version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.multi
    ports:
      - "8000:8000"
    depends_on:
      - cache
  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"
